#ifndef HOLBERTON_H
#define HOLBERTON_H

/*function that creates an array of chars, and initializes it
 * with a specific char*/
char *create_array(unsigned int size, char c);

/*function that returns a pointer to a newly allocated space*/
char *_strdup(char *str);

#endif
